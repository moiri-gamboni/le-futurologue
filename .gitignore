# design & plan
/docs
# full size pictures
/src/lib/assets/images/originals

node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
# Catch-all for any *.env credential files (e.g. .brevo.env)
*.env
!example.env

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rodney (browser automation scoped to this dir)
.rodney/

# Brand source kit — raw PDFs, AI files, original photos, fonts.
# Too big for git. The subsetted woff2s + curated brand assets that
# actually ship live under static/fonts/ and src/lib/assets/brand/.
/artistic-direction/

# Design-phase analysis artifacts (JSON dumps, Python exploration
# scripts, session notes). Kept locally for reference; not part of
# the runtime.
/scripts/

# Local plan files (per-feature implementation plans).
/plans/
