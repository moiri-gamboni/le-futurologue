# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# shadcn-svelte vendored components — kept as the CLI emits them so
# `npx shadcn-svelte add` upgrades apply without reformatting conflicts.
/src/lib/components/ui/

# Brand source kit + analysis artifacts (both gitignored locally)
/artistic-direction/
/scripts/
/plans/

# Generated / editor
/.claude/
/.rodney/
/.svelte-kit/
